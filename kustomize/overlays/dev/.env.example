# Create an env file like this (values are throwaway and for local dev, not sensitive so you can copy paste)

# ===== DATABASE CONFIGURATION =====
# Using in-cluster MariaDB service for dev
DATABASE_HOST=mariadb
DATABASE_PORT=3306
DATABASE_NAME=specify_dev
MASTER_NAME=specify
MASTER_PASSWORD=specify_pass

# MariaDB root password (none)
MYSQL_ROOT_PASSWORD=

# ===== SECURITY =====
SECRET_KEY=local-dev-secret-key-not-for-production-12345678

# ===== ASSET SERVER =====
# Using in-cluster asset-server service (no Nginx needed in Kubernetes)
ASSET_SERVER_URL=http://asset-server:8080/web_asset_store.xml
ASSET_SERVER_KEY=test-asset-key

# ===== WEB CONFIGURATION =====
# For local access via port-forward
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://localhost

